/**
 * Configuration utilities barrel export
 * Configuration management and target setup
 */

// Target configuration
export { default as TargetConfig } from '../../target-config.js';
export { default as TargetUtils } from '../../target-utils.js';

// MCP configuration
export { default as McpConfig } from '../../mcp-config.js';

// Path utilities
export * from '../../paths.js';

// JSONC utilities for configuration files
export * from '../../jsonc.js';